Those scripts were written to format "incbin" or "include" directives automatically on split data structures of the disassembly.

The script's loop structure then depends on the data structure's content.

When doing such a task manually is too tedious, you can make your own script by taking example on one the existing ones.